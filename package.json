{
  "name": "frontend-velox",
  "version": "0.0.0",
  "private": true,
  "description": "Velox frontend (Creator Studio + YouTube Manager + Drive + Livestream + Dark Editor). Produces the SPA dist consumed by the Velox master server via VELOX_SPA_DIR.",
  "scripts": {
    "build": "node scripts/run-if-web-manifest.js build",
    "lint": "node scripts/run-if-web-manifest.js lint",
    "test": "node scripts/run-if-web-manifest.js test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "UNLICENSED"
}
